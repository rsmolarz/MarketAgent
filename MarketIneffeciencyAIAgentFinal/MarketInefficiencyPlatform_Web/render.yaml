services:
  - type: web
    name: market-inefficiency-platform
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ui.server:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: PORT
        value: 10000