run = "bash -lc 'cd MarketInefficiencyPlatform_Web && pip install -r requirements.txt && (python -u main.py --agents all --mode realtime &) && python -m uvicorn ui.server:app --host 0.0.0.0 --port $PORT'"


